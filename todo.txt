x ou X : "abcdef" ou "ABCDEF" (toupper?)
cas d'erreur : string vide, arg vide, arg mauvais type, string null;
%% et \%
n$ pour utiliser n fois le parametre ?
chopper bon ft_strlen, memcpy, memset, ft_strnew.
modifier ft_putstr pour aller vite.
return negative value if problem ; else return num of char printed
"%.s" printf -> pas de segf
Makefile : regle d, regle fsanitize, regle valgrind
